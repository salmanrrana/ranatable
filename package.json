{
  "name": "ranatable",
  "private": true,
  "type": "module",
  "scripts": {
    "lint": "oxlint",
    "format": "prettier --write",
    "test": "node --test tests/unit.test.mjs tests/gesture-controls.test.mjs tests/hands.test.mjs",
    "test:browser": "node --test tests/browser.test.mjs",
    "check:fast": "pnpm lint && pnpm test",
    "check:staged": "node scripts/check-staged.mjs",
    "prepare": "node scripts/install-hooks.mjs"
  },
  "devDependencies": {
    "oxlint": "^1.79.0",
    "playwright": "1.58.2",
    "prettier": "^3.6.2"
  }
}
